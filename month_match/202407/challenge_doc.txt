crypto:rsa_gcd
1) rsa算法
2) 给出的两组中n存在公因数
3) 低
misc:picture
1) 文件格式解码
2) 观察程序二进制文件，观察程序文件头字段，提取隐藏数据文件
3) 中
pwn:100levels
1) 函数的栈溢出，开启pie的rop
2) 栈溢出覆盖掉当前函数保存的上一个函数的rbp的低位，使其往上偏移12个字节，这样就可以打印出返回地址的低4位
3) 高
re:logmein
1) 算法逆向
2) 输入数据长度要等于 v8 的长度（截止符也在长度中），且根据转换后必须全部相等，否则显示错误
3) 中
web:fakebook:
1) php注入
2) 发现view.php/?no=存在注入可能，后根据注入结果构造反序列化POC，注入后翻看源码获得flag
3) 中